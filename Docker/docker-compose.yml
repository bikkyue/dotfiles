services:
  dotfiles:
    build:
      context: .
      dockerfile: dockerfile
    container_name: dotfiles-dev
    volumes:
      - .:/home/user/dotfiles
    environment:
      - TERM=xterm-256color
    tty: true
    stdin_open: true
    command: bash

